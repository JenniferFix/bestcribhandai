import React from "react";
import { useMutation } from "@tanstack/react-query";
import { anthropicClient } from "@/lib/anthropic";

const CardResults = () => {
  return <div>CardResults</div>;
};

export default CardResults;
